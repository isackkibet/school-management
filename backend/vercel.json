{
  "buildCommand": "npx prisma generate",
  "installCommand": "npm install",
  "outputDirectory": ".",
  "functions": {
    "api/index.js": {
      "memory": 256,
      "maxDuration": 10
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "api/index.js" }
  ]
}
